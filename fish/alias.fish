# vim -> nvim
alias vim='nvim'

# GitHub
alias hb="hub browse"

# Git
alias g='git'
alias ga='git add'
alias gc='git commit --verbose'
alias gp='git push'

# Cmd
alias ll='ls -a -l -h -p'

#Reboot shell
alias sh_reboot='exec $SHELL -l'

#Setting_dotfile
alias dotfile='code ~/dotfiles/'
